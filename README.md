### AWS CLI Setup

```sh
    sudo apt update
    sudo apt install awscli
    aws --version
```

## Create a Profile ENV (optional)

> Create a .env file based on the sample env.

## Load the ENV

```sh
    source .env
```
